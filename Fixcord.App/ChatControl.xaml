<UserControl x:Class="Fixcord.App.ChatControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Fixcord.App"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="20" />
			<RowDefinition Height="*" />
			<RowDefinition Height="20" />
		</Grid.RowDefinitions>

		<Button Content="Refresh" Click="Button_Click" Grid.Row="0" />
		<ListBox x:Name="messages" Grid.Row="1" />
		<TextBox x:Name="messageInput" Grid.Row="2" KeyDown="MessageInput_KeyDown" />
	</Grid>
</UserControl>
